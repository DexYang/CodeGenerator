[
  <% data.list.forEach(row => {
    var keys = Object.keys(row)
  -%>
  {
  <% for (var j = 0; j < keys.length; j++ ) { -%>
    "<%= keys[j] %>": <% if (typeof row[keys[j]] == 'number') { -%>
<%= row[keys[j]] -%>
<% } else { -%>
"<%= row[keys[j]] %>"<% } %><% if(j < keys.length - 1) { %>,<% } %>
  <% } -%>
  },
  <% }) %>
]
