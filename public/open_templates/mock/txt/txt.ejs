<% data.list.forEach(row => {
  var keys = Object.keys(row)
-%>
<% for (var j = 0; j < keys.length; j++ ) { -%>
<%= row[keys[j]] -%>
<% if(j < keys.length - 1) { -%>
<%= variables.__mock__separator -%>
<% } -%>
<% } %>
<% }) %>
