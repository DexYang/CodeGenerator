<% data.list.forEach(row => {
  var keys = Object.keys(row)
-%>
insert into `<%= variables.__mock__tableName %>` (<% -%>
<% for (var j = 0; j < keys.length; j++ ) { -%>
`<%= keys[j] %>`<% if(j < keys.length - 1) { %>,<% } -%>
<% } -%>
) value (<% -%>
<% for (var j = 0; j < keys.length; j++ ) { -%>
<% if (typeof row[keys[j]] == 'number') { -%>
<%= row[keys[j]] -%>
<% } else { -%>
'<%= row[keys[j]] %>'<% } if(j < keys.length - 1) { %>,<% } -%>
<% } %>);
<% }) %>
